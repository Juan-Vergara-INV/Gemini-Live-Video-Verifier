services:
  - type: web
    name: gemini-video-verifier
    env: python
    buildCommand: |
      apt-get update && apt-get install -y gfortran
      pip install -r requirements.txt
    startCommand: streamlit run video_analyzer.py --server.port $PORT --server.address 0.0.0.0
